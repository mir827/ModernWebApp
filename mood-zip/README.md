# MOOD ZIP

하루를 한 줄, 하나의 무드, 에너지 점수로 압축하는 모바일 우선 마이크로 저널입니다.

## 특징

- 로그인·서버 없이 브라우저 `localStorage`에만 저장
- 최근 7일 에너지 리캡 및 연속 기록일 표시
- 공유용 주간 요약 문구 생성
- 모바일에 최적화된 단일 페이지 웹앱

## 실행

`index.html`을 브라우저에서 열거나 정적 웹 서버로 실행합니다.

## 배포

GitHub Pages의 배포 원본을 `main` 브랜치의 `/mood-zip` 폴더로 지정하면 배포할 수 있습니다.
